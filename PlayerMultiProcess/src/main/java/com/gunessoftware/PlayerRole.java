package com.gunessoftware;

/**
 * This enum is used to define role of the player in terms of message communication.
 */
public enum PlayerRole {
    SENDER,
    RECEIVER,
    SENDER_RECEIVER
}
